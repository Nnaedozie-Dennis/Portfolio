// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
//   experimental: {
//     turbo: false, // ⛔ disable Turbopack (fixes the font error)
// },
// };

// export default nextConfig;


import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    turbo: false, // ⛔ disable Turbopack (fixes the font error)
  },
};

export default nextConfig;
